# Grade Checker — Prototype

Static, client-side academic record / GPA tracker built to pitch to QUT College.
No backend, no build step — just static HTML/CSS/JS.

## Local preview
Open `index.html` directly in a browser, or run:
    npx serve .

## Deploy
See main conversation for Vercel deployment steps.
